import fs from 'fs';
import path from 'fs'; // Just node standard library

// We will use standard string replacements.
// Since we have specific files, we can just use `sed` or node to do precise string replacements.
